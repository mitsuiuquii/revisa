{
  "buildCommand": "cd backend && pip install -r requirements.txt",
  "outputDirectory": "backend",
  "env": {
    "PYTHONUNBUFFERED": "1"
  },
  "functions": {
    "api/index.py": {
      "maxDuration": 60
    }
  }
}
