{
  "name": "revisa",
  "version": "3.0.0",
  "private": true,
  "scripts": {
    "build": "yarn --cwd frontend install && yarn --cwd frontend build && rm -rf build && cp -R frontend/build build"
  }
}
